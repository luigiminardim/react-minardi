/* Imported url for fonts are in the './fonts.css' file, which is imported in '@pages/_app.tsx'.
   Make sure that all the fonts needed are imported in './fonts.css'.
*/

import styled from 'styled-components'
import { H1 as _h1 } from '../../abstract-components/Text'

export const H1 = styled(_h1)`
  font: 400 14px/1 Roboto, sans-serif;
`
